number = 20

if number > 30
  puts "この数値は30より大きいです。" 
else
  puts "この数値は30より小さいです。"    
end

#解説
#else節を使うことで、条件が偽の場合に別の処理を実行することができる。
#今回の例はnumberの値が20に対して条件が「30より大きいか」なので、偽として処理され、else節のとして「この数値は30より小さいです。」と表示される。